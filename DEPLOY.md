# Deploy info

**Live URL:** https://ineffable2929.github.io/swedish-for-leighcie/

**Host:** GitHub Pages
**Repo:** https://github.com/Ineffable2929/swedish-for-leighcie (public)
**Branch / path:** `main` / `/` (root)
**First deployed:** 2026-06-07

## To update the live site
```
cd /Users/orbit/swedish-for-leighcie
git add -A
git commit -m "describe the change"
git push
```
Pages rebuilds automatically (~1 minute). Hard-refresh on the phone if you
don't see the change immediately.

## Optional: move to Vercel later
1. Go to vercel.com → Add New → Project → Import `Ineffable2929/swedish-for-leighcie`.
2. Framework preset: **Other** (it's plain static). Build command: none.
   Output directory: leave as the repo root.
3. Deploy. You'll get a `*.vercel.app` URL (and can add a custom domain).
Pushing to GitHub will then auto-deploy to Vercel too.
